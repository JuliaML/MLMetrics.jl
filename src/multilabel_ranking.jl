function coverage_error(y_true::Array, y_pred::Array)
    return(true)
end

function label_ranking_average_precision_score(y_true::Array, y_pred::Array)
    return(true)
end

function label_ranking_loss(y_true::Array, y_pred::Array)
    return(true)
end
