function adjusted_mutual_info_score(y_true::Array, y_pred::Array)
    return(true)
end

function adjusted_rand_score(y_true::Array, y_pred::Array)
    return(true)
end

function completeness_score(y_true::Array, y_pred::Array)
    return(true)
end

function homogeneity_completeness_v_measure(y_true::Array, y_pred::Array)
    return(true)
end

function homogeneity_score(y_true::Array, y_pred::Array)
    return(true)
end

function mutual_info_score(y_true::Array, y_pred::Array)
    return(true)
end

function normalized_mutual_info_score(y_true::Array, y_pred::Array)
    return(true)
end

function silhouette_score(y_true::Array, y_pred::Array)
    return(true)
end

function silhouette_samples(y_true::Array, y_pred::Array)
    return(true)
end

function v_measure_score(y_true::Array, y_pred::Array)
    return(true)
end
