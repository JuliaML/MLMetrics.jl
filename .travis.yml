language: julia
os:
  - linux
  - osx
julia:
  - nightly
  - 0.4
notifications:
  email: false
script:
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("Metrics"); Pkg.test("Metrics"; coverage=true)'